spring.application.name=mokkoji

# MySQL DB ??
spring.datasource.url=jdbc:mysql://localhost:3306/localmap_db?useSSL=false&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=yourpassword

# JPA ??
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ?? ??? (????)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT ???
jwt.secret=your_jwt_secret_key

# OAuth - Kakao ?? (??)
oauth.kakao.client-id=???RESTAPI?
oauth.kakao.redirect-uri=http://localhost:8080/auth/kakao/callback
